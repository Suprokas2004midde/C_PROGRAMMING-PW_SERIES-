#include<stdio.h>
int main()
{
    char arr[20]="Hellow world";
    arr[0]=107;
    int i=0;
    while (arr[i]!='\0')
    {
        printf("%c",i[arr]); // here we can reverse them,, but the output will be same....
        i++;
    }
    // printf("\n");
    // printf("%c",arr[5]);
    return 0;
}