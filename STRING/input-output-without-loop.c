#include<stdio.h>
#include<string.h> // this header file to call gets(),puts() library function.....
int main()
{
    char arr[]="Hi! i am a coder..";

    //printf("%s",arr); // %s is a format specifier for strings....
    puts(arr); // if we want to print a string we can use puts value... words between parenthess will print....
    return 0;
}